<!DOCTYPE html>
<html lang="en">
<head>
    <title>Profile ejs</title>
</head>
<body>

    <%- include('common/header')%>
    <h1>profile ejs rendered <%= user.name %></h1>

    <ul>
        <% user.skills.forEach((item) => { %>

            <li><%= item %></li>

        <% }) %>
    </ul>
</body>
</html>